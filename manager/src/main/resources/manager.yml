id: manager1
vpn-ip: 172.27.232.16

cloud-communication-endpoint:
  host: localhost
  port: 40052
  protocol: THRIFT_TCP

cloud-cluster-endpoint:
  port: 40012
  protocol: THRIFT_TCP

cloud-library-endpoint:
  port: 40042
  protocol: THRIFT_TCP

library-endpoint:
  host: localhost
  port: 40042
  protocol: THRIFT_TCP

def-servers:
  rest:
    enabled: true
    port: 40000
    bind-address: 0.0.0.0
    url-pattern: /api/*
  thrift-http:
    enabled: false
    port: 40001
    bind-address: 0.0.0.0
    url-pattern: /api/*
  thrift-tcp:
    enabled: true
    port: 40002
    bind-address: 0.0.0.0
